#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../typedoc/bin/typedoc" "$@"
else
  exec node  "$basedir/../../../typedoc/bin/typedoc" "$@"
fi
# cmd-shim-target=C:/Users/manik/Downloads/consensus-ai-source/node_modules/.pnpm/typedoc-plugin-coverage@4.0_cbbf18d397e46f53c41dddf561529b6e/node_modules/typedoc/bin/typedoc
